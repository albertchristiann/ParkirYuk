<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorOrange</item>
        <item name="colorPrimaryDark">@color/colorOrange</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="fontFamily">@font/montserrat_light</item>
        <item name="backgroundColor">@color/colorWhite</item>
        <item name="cardBackgroundColor">@color/colorWhite</item>
        <item name="textColor">@color/colorBlack</item>
        <item name="textNavColor">@color/colorWhite</item>
        <item name="navHeadColor">@color/colorGray</item>
        <item name="tintColor">@color/colorBlack</item>
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="buttonColor">@color/colorBlack</item>
        <item name="buttonTextColor">@color/colorWhite</item>
    </style>

    <!-- Dark theme -->
    <style name="darkTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/darkOrange</item>
        <item name="colorPrimaryDark">@color/darkOrange</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="backgroundColor">@color/colorGray</item>
        <item name="cardBackgroundColor">@color/colorBlack</item>
        <item name="fontFamily">@font/montserrat_light</item>
        <item name="navHeadColor">#DFDFDF</item>
        <item name="textColor">@color/colorWhite</item>
        <item name="textNavColor">@color/colorBlack</item>
        <item name="tintColor">@color/colorWhite</item>
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="buttonColor">@color/colorWhite</item>
        <item name="buttonTextColor">@color/colorWhite</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="NavigationText" parent="@android:style/TextAppearance.Medium">
        <item name="android:textSize">12sp</item>
        <item name="android:fontFamily">@font/montserrat_light</item>
    </style>
    
    <style name="WindowAnimationTransition">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>
</resources>